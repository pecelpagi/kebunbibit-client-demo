import { styled } from '../../stitches.config';

export const EditButton = styled('button', {
    color: '#F04630',
    fontSize: '0.875rem',
    lineHeight: '1.25rem',
    '&:hover': {
        textDecoration: 'underline',
    }
});
