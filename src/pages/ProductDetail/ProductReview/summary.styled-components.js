import { styled } from '../../../stitches.config';

export const StatisticProgressBar = styled('div', {
    width: '100%',
    height: '5px',
    background: '#8bc64e',
    borderRadius: '4px',
    position: 'relative',
});