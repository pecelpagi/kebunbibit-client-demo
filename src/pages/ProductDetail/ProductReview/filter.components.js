import { StarFilledIcon } from '@radix-ui/react-icons';

export const Icon = () => (
    <span style={{
        color: '#ffc600',
        position: 'relative',
        bottom: 1,
        marginRight: '2px'
    }}>
        <StarFilledIcon height={14} width={14} />
    </span>
)
